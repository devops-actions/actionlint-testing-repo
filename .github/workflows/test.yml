on:
  push: 

  workflow_dispatch:

permissions:
  contents: read
  pull-requests: write

jobs:
  job-1:
    runs-on: ubuntu-latest
    steps:
    - run: |
       #curl -sS -f -I -H "Authorization: token ${{ secrets.GITHUB_TOKEN }}" https://api.github.com
       #echo "-------------------------------------------------------------------------------------"
       temp=$(curl -sS -f -I -H "Authorization: token ${{ secrets.GITHUB_TOKEN }}" https://api.github.com | grep ^x-oauth-scopes:)
       echo "-------------------------------------------------------------------------------------"
       echo $temp
       echo "-------------------------------------------------------------------------------------"
       echo $temp | cut -d' ' -f2- | tr -d "[:space:]" | tr ',' '\n'
       echo "-------------------------------------------------------------------------------------"
       
    - uses: actions/checkout@v3
    - uses: devops-actions/actionlint@update2
